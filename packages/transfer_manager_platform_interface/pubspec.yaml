name: transfer_manager_platform_interface
description: Platform contract for native transfer_manager implementations.
version: 2.1.0
homepage: https://github.com/pinzarulab/transfer_manager
repository: https://github.com/pinzarulab/transfer_manager
issue_tracker: https://github.com/pinzarulab/transfer_manager/issues

environment:
  sdk: ^3.8.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
